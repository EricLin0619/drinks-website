export type AddDrinkButtonType = {
    name: string;
    price: number;
    imageUrl: string;
}

export type DrinkCardType = {
    name: string;
    price: number;
    imageUrl: string;
    description: string;
}